export default function Dashboard({title}) {
    return(
        <div>
            <h1>
                {title}
            </h1>
            <p>
                This is dashboard!
            </p>
        </div>
    )
}